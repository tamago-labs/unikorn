{
  "name": "unikane",
  "version": "0.1.0",
  "private": true,
  "description": "Code is truth. Kane is proof. Tutorial + marketing from your real codebase, verified.",
  "type": "module",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "verify": "node scripts/verify.mjs",
    "kane:ingest": "kane-cli context ingest ./PRD.md --mode agent",
    "kane:extract": "kane-cli context extract --mode agent",
    "kane:design": "kane-cli design tests --mode agent",
    "kane:cover": "kane-cli cover",
    "kane:demo": "kane-cli run \"Go to http://localhost:3000/deck/demo, click Next through 5 slides, assert the TAM calculator updates when input changes, assert no console errors and no API calls returned 5xx\" --agent --headless"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
