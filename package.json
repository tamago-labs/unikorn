{
  "name": "unikorn",
  "version": "0.1.0",
  "private": true,
  "description": "Code is truth. Kane is proof. Tutorial + marketing from your real codebase, verified.",
  "type": "module",
  "bin": {
    "unikorn": "dist/index.js"
  },
  "files": ["dist", "frontend/dist", "README.md"],
  "scripts": {
    "dev": "concurrently -n cli,web -c blue,green \"npm run dev:cli\" \"npm run dev:web\"",
    "dev:cli": "tsx src/index.ts",
    "dev:web": "npm --prefix frontend run dev",
    "build": "tsc && npm --prefix frontend run build",
    "start": "node dist/index.js",
    "kane:ingest": "kane-cli context ingest ./PRD.md --mode agent",
    "kane:extract": "kane-cli context extract --mode agent",
    "kane:design": "kane-cli design tests --mode agent",
    "kane:cover": "kane-cli cover",
    "kane:demo": "kane-cli run \"Go to http://localhost:3000, assert hero title is 'What did you build today?', fill input with 'https://github.com/tamago-labs/everclaw', click Generate, assert no console errors\" --agent --headless"
  },
  "dependencies": {
    "express": "^4.21.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.0",
    "concurrently": "^8.2.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
